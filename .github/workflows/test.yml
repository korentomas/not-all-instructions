name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -e ".[analysis,dev]"
      # fast suite (pyproject addopts excludes the slow MCMC tests); offline - 
      # mockllm models only, no API keys, no cloned codebases needed.
      - run: pytest
